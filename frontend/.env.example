# Copie este arquivo para .env e ajuste os valores para o seu ambiente local.
# NUNCA envie o .env real para o repositório.

# URL base da API Django (sem barra no final)
# Em desenvolvimento, aponta para o servidor local do Django.
# Em produção, troque pelo domínio real, ex: https://api.labic.edu.br
VITE_API_URL=http://localhost:8000
